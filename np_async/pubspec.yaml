name: np_async
description: A new Flutter package project.
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ">=3.9.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  np_collection:
    path: ../np_collection

dev_dependencies:
  dart_style: 3.1.2
  np_lints:
    path: ../np_lints
